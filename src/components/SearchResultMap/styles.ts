import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  map: {
    width: "100%",
    height: 400,
    backgroundColor: "red",
  },
  markerUber: {
    width: 30,
    height: 30,
  },
});

export default styles;
