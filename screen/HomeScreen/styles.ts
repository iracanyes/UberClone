import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "red",
    paddingTop: 0,
    marginTop: 0,
  },
  map: {
    width: "100%",
    height: 400,
    zIndex: 1,
  },
});

export default styles;
