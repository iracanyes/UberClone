import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 450,
  },
  map: {
    width: "100%",
    height: "100%",
    zIndex: 1,
  },
});

export default styles;
